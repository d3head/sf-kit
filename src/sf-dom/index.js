import querySelector from './querySelector';
import querySelectorAll from './querySelectorAll';
import { DOCUMENT, BODY, WINDOW, HTML} from './globals';

export default {
  querySelector,
  querySelectorAll,
  DOCUMENT,
  BODY,
  WINDOW,
  HTML,
};
